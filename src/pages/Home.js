import React from 'react';
import {
    Typography,
} from '@material-ui/core';

// eslint-disable-next-line import/no-anonymous-default-export
export default () => (
    <Typography variant="h4">Okay it works now!</Typography>
);